import React from 'react';
import styled from 'styled-components';

export const ListHeader = styled.div`
    display: flex;
    border-radius: 1px;
    justify-content: flex-start;
    font-weight: bold;
    align-items: center;
    width: auto;
    height: auto;
    transition: visibility 2ms ease-in-out;
`;